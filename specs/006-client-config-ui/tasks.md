# Tasks: Tauri Client Configuration UI

**Input**: Design documents from `specs/006-client-config-ui/`

**Prerequisites**: plan.md ✅ spec.md ✅ research.md ✅ data-model.md ✅ contracts/ ✅ quickstart.md ✅

**Tests**: INCLUDED — constitution VI requires automated tests for new Tauri commands (unit: device-not-found, PTT timeout, voice format/duration) and hub endpoints (unit: LLM config Redis read, voice profile validation).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel with other [P] tasks in the same phase (different files, no shared dependencies)
- **[Story]**: Which user story this task belongs to (US1–US7)
- Exact file paths are relative to repo root

> **D2 — SC numbering disambiguation (read before implementing)**: `SC-N` (single-digit, e.g., SC-7) = `quickstart.md` end-to-end validation scenario. `SC-NNN` (three-digit, e.g., SC-007) = `spec.md` success criterion. These are DIFFERENT sequences with overlapping numbers. Example conflict: quickstart SC-7 = "voice profile upload E2E test"; spec SC-007 = "settings persist across restart." When a task references a success criterion, verify the digit count before assuming which document is meant.

> **F1 — plan.md phase-letter cross-reference**: plan.md's Implementation Phases use letters A–H (dependency flow); this file uses Phase 1–9 (story-priority order — authoritative for execution). Mapping: A=1 (Setup), D=2 (US1 Audio), B=3 (US2 Connection/LLM), C=4 (US3 PTT), Phase 5 (US4 Personality) has no plan letter (verification-only), E=6 (US5 Debug), F=7 (US7 Logging), G=8 (US6 Voice), H=9 (Polish). When plan.md says "Phase B," it means Phase 3 here.

## Story priority map (from spec.md)

| Story | Priority | Theme |
|-------|----------|-------|
| US1 | P1 🎯 MVP | Audio device configuration |
| US2 | P1 | Connection configuration + LLM fields |
| US3 | P2 | PTT hotkey interactive binding |
| US4 | P2 | Engineer personality sliders |
| US5 | P2 | Telemetry debug readout |
| US6 | P3 | Voice profile upload |
| US7 | P3 | Telemetry logging toggle |

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: AppConfig extension, new dependencies, Docker volume, and tab component — prerequisite before any story work begins. No story work starts until T010 passes.

- [X] T001 Extend `AppConfig` in `apps/tauri-client/src-tauri/src/state.rs`: add `llm_base_url: String` (default: `"https://lemonade.tdkottke.com/v1"`), `llm_model: String` (default: `"user.Ornith-1.0-35B-GGUF"`), `llm_api_key: String` (default: `""`), `telemetry_logging_enabled: bool` (default: `false`), `telemetry_log_dir: String` (sentinel default: `""` in `Default::default()` — `app_data_dir()` requires a Tauri app handle and cannot be called from a `Default` impl; in the app startup init flow, if `telemetry_log_dir == ""`, resolve it to `app_handle.path().app_data_dir()?.join("logs/telemetry").to_string_lossy().into_owned()` and immediately persist the resolved path to the saved config so subsequent reads always have an absolute path); update `Default` impl; add `#[serde(default)]` on all new fields for backward compatibility with existing saved configs; also add `first_launch_seen: bool` (default: `false`) — set to `true` after the first successful Save to suppress the first-launch LLM hint after successful onboarding (F1 — `false` is what displays the hint; `true` suppresses it; "trigger" was a misleading label). **C2 (read-only at UI layer)**: `telemetry_log_dir` is stored in `AppConfig` so it persists across restarts, but in M10 it is not exposed as an editable field in the UI (FR-019: read-only display only). The field exists in the struct for future editability; the default value is authoritative for this milestone. **E2 (AppConfig round-trip test)**: Add a Rust unit test in `state.rs` tests: serialize a fully-populated `AppConfig` (all new M10 fields set to non-default values) to JSON → deserialize → assert every field retains its value. Also deserialize an old-format JSON string (missing all new fields) and assert each new field gets its `#[serde(default)]` value — explicitly verify: `llm_base_url == "https://lemonade.tdkottke.com/v1"`, `llm_model == "user.Ornith-1.0-35B-GGUF"`, `llm_api_key == ""`, `telemetry_logging_enabled == false`, `telemetry_log_dir == ""`, and `first_launch_seen == false`. This guards against silent data loss when field names or types change in future milestones. **I1 (ptt_hotkey default change — verified conflict, 2026-07-08)**: the existing `ptt_hotkey` field defaults to `"F13"` in M5's `AppConfig::default()` (state.rs:49), but FR-012/A2, US3 scenario 3, T023/F4, and `data-model.md` all define `""` as the never-configured sentinel — with an `"F13"` default, the never-configured soft prompt is unreachable and US3 scenario 3 is untestable. As part of T001, change the `ptt_hotkey` default to `""`. With `#[serde(default)]` semantics, configs saved by M5 retain their explicitly saved `"F13"` value on load — only truly fresh installs get `""`. ALSO update the startup PTT registration path to SKIP global-shortcut registration when `ptt_hotkey == ""` (never-configured is a valid state, not an error — no FR-012 banner; the Hotkeys tab shows the T023/F4 soft prompt instead). Add to the E2 round-trip test: old-format JSON missing `ptt_hotkey` deserializes to `""`, and a fresh `AppConfig::default()` has `ptt_hotkey == ""`.
- [X] T002 Remove deprecated M4 stub fields `chattiness: String`, `familiarity: String`, `aggression: String` from `AppConfig` in `apps/tauri-client/src-tauri/src/state.rs` — grep for remaining call sites in `apps/tauri-client/` and remove them. Confirm zero references before deleting.
- [X] T003 [P] Add `music-metadata` to `apps/hub-server/package.json` (for MP3 duration validation); run `npm install` at repo root.
- [X] T004 [P] Add `chatterboxReferenceAudioDir: "/data/chatterbox/reference"` field to `apps/hub-server/config/engineer-config.json` and update the `EngineerConfig` TypeScript type that reads this file. **D3 (Constitution IV — LLM fallback defaults) — U2 UPDATE (verified 2026-07-08)**: the `llm` section ALREADY EXISTS in `engineer-config.json` with `baseUrl: "https://lemonade.tdkottke.com/v1"`, `model: "user.Ornith-1.0-35B-GGUF"`, plus `provider`, `timeoutMs`, `maxResponseTokens`, `tokenBudget` (and no `apiKey` — correct). This sub-step is therefore a VERIFICATION, not an addition: confirm the `EngineerConfig` TypeScript type covers the existing `llm` fields and extend the type if it lags the JSON. This is the fallback source T018 reads when `hub:config:llm` is absent from Redis — if the type omits the section, T018's fallback path breaks at compile time. **U1 (voice-profile duration constants — creation assigned HERE, verified absent 2026-07-08)**: ALSO add `minVoiceProfileDurationSecs: 3` and `maxVoiceProfileDurationSecs: 60` to `engineer-config.json` and include both in the `EngineerConfig` type. FR-022 references these named constants but no task previously CREATED them — T034b's step (c) only "updates" them (which presumes existence) and T034 requires them already recorded before it starts. Creation happens here in Phase 1 with the assumed 3–60s values; T034b revises the VALUES if the Chatterbox confirmation finds different limits.
- [X] T005 Add a named shared volume (e.g., `chatterbox-reference-audio`) to `infra/docker-compose.yml` mounted into both the `hub-server` and `chatterbox` services at the configured reference audio path. **Constitution IV gate: this MUST be present before Phase 8 (US6) hub code references the directory** (F2 — corrected from "Phase 7", a stale label from a prior renumbering; Phase 7 is US7 Logging). **B3 (ordering note)**: T005 is NOT marked `[P]` — it is a critical-path Phase 1 task because Phase 8 (US6 voice profile hub endpoint) directly depends on this volume being present. T005 MUST be completed in Phase 1, before Phase 8 begins; do not defer it within Phase 1's parallel work window. T034b provides a runtime verification gate for the volume mount before Phase 8 implementation starts.
- [X] T006 Implement `SettingsTabs` component in `packages/ui/src/components/SettingsTabs/index.tsx` — tab bar (horizontal tab labels) + content area; accepts `tabs: Array<{ id: string; label: string; content: ComponentChildren }>` prop; active tab state in local component state; preserves the parent's unsaved form state object across tab switches (parent owns state, tabs are display-only).
- [X] T006b Unit tests for `SettingsTabs` in `packages/ui` (**C1 — Constitution VI**, which names mocha + chai for `packages/ui` component contracts; depends on T006). `packages/ui` currently has NO test infrastructure (verified 2026-07-08: no test script, no test deps, zero test files) — bootstrap it as part of this task: add `mocha`, `chai`, `tsx`, `jsdom`/`global-jsdom`, and `@testing-library/preact` as devDependencies with a `"test": "mocha --import=tsx 'test/**/*.test.tsx'"` script (mirror the existing mocha setup in `packages/types`). Then add `packages/ui/test/SettingsTabs.test.tsx` covering the component contract: (1) renders one tab label per entry in the `tabs` prop; (2) clicking a tab label switches the visible content area to that tab's content; (3) active-tab state defaults to the first tab; (4) **parent-state preservation** — render with a stateful parent harness, edit a value rendered inside tab A's content, switch to tab B and back, assert the value is unchanged (this is the load-bearing contract behind the cross-tab unsaved-state edge case / B4). **Framework note (C1)**: Constitution VI mandates mocha + chai here — consistent with `packages/types` and `apps/hub-server` (the repo has no Vitest anywhere yet; T009 introduces Vitest only for `apps/tauri-client`, which Constitution VI does not name). Do NOT use Vitest for this package without first obtaining a constitution PATCH amendment (project-owner approval).
- [X] T007 [P] Export `SettingsTabs` from `packages/ui/src/index.ts`.
- [X] T008 Restructure `apps/tauri-client/src/pages/Setup.tsx` to use `SettingsTabs` with 7 tabs (Audio, Connection, Hotkeys, Personality, Debug, Voice, Logging). Move all existing Setup.tsx sections into the appropriate tabs — Audio tab gets existing device dropdowns + AudioDeviceTestPanel; Connection tab gets existing Redis/hub URL fields; Personality tab gets existing PersonalityPanel; remaining tabs are placeholder content areas to be wired in later phases. Lift all form fields into a single `formState` object to preserve values across tab switches. **C1 (FR-001 — navigation link verification)**: As part of restructuring, verify that a navigation entry to the Settings page exists in the Tauri router config (e.g., `apps/tauri-client/src/router.tsx` or equivalent) and in any nav components (sidebar, header, menu). Run `grep -r "Setup\|settings\|/settings" apps/tauri-client/src/` to identify all navigation reference sites. If a nav entry is absent (i.e., Settings is unreachable from the main UI without directly editing the URL), add it now as part of this task — FR-001 requires the settings panel to be accessible from the main navigation. Document the grep result and the nav entry status in the PR description. **U3 (connection_token preservation — FR-025 / FR-007-E4, verified 2026-07-08)**: the current M4 `Setup.tsx` hardcodes `connection_token: ''` in its save payload (Setup.tsx:114) — every Save silently wipes a stored M4 token, violating FR-025's no-data-loss requirement. When lifting fields into `formState`, initialize `formState.connection_token` from `get_config()` like every other field and pass it through unchanged on Save; it stays hidden from the UI per FR-007/E4.
- [X] T009 [P] Update `apps/tauri-client/src/pages/Diagnostics.tsx` to be a thin redirect/wrapper to the Debug tab of Setup (navigate to Setup with `?tab=debug` query param). Preserves existing navigation links. **D2 / C4 (Constitution VI — test required)**: Add the following step to quickstart.md SC-5b: "1. Navigate to the `/diagnostics` route directly (e.g., enter it in the Tauri WebView URL bar or click a previously saved nav link). 2. Confirm the app lands in the Settings panel with the Debug tab active. 3. Confirm the debug content (session status, telemetry variables, stream lag) is visible. Pass: correct redirect with Debug tab shown. Fail: blank page, 404, or Debug tab not active." **I5 (already applied)**: quickstart.md already contains scenario SC-5b with this content (verified 2026-07-08) — before closing out T009, VERIFY it still matches this text rather than adding it again; SC-5b (not SC-5) is the correct target scenario. **D2 (Constitution VI — automated test required)**: Before implementing the redirect, run `grep -r '/diagnostics' apps/tauri-client/src/` and list all reference sites — confirm each will be covered by the redirect wrapper (F3). Then add an automated route test: if a Playwright or Tauri WebDriver test suite exists, add a test that navigates to `/diagnostics` and asserts the Debug tab is active; if no E2E suite exists, add a Vitest unit test for `Diagnostics.tsx` that renders the component with a mocked router and asserts it renders a `<Navigate>` redirect to `/settings?tab=debug` (or equivalent) — or asserts that `navigate('/settings', { state: { tab: 'debug' } })` was called. **A1 (route name not yet pinned)**: the settings page component is `Setup.tsx` — the actual route path may be `/setup` rather than `/settings`; the F3 grep plus T008/C1's router-config inspection determine the real path. Pin the test's asserted target to the actual route once confirmed; `/settings` above is illustrative, not normative. A "DEBT:" comment in T041 is NOT acceptable — Constitution VI designates test-backed change as NON-NEGOTIABLE. The redirect is a behavioral change and MUST have automated coverage at some test layer before merge. **F2 (grep output is mandatory)**: The `grep -r '/diagnostics' apps/tauri-client/src/` result MUST be explicitly documented in the PR (either as a comment or as a note in T040/E2) — zero results is a valid outcome ("no pre-existing /diagnostics links found") and must be stated, not silently skipped. Undocumented grep output is not considered complete. **E3 (Vitest test infrastructure prerequisite)**: Before implementing the automated redirect test, verify that a Preact/Vitest test setup exists in `apps/tauri-client/` (check for `vitest.config.ts` or equivalent). If it does NOT exist, initialize Vitest for the Tauri client as part of T009: add `vitest.config.ts` with JSDOM environment, add `@testing-library/preact` as a dev dependency, confirm `npm test` in `apps/tauri-client/` runs a no-op suite. This infrastructure is required for the Constitution VI-mandated redirect test — T009 cannot be closed without it.
- [X] T010 Run `npm run build && npm run typecheck` workspace-wide + `cargo check` + `npm test --workspace=apps/tauri-client` + `npm test --workspace=packages/ui` — all MUST pass before Phase 2. **D1 (Constitution VI gate)**: T009 initializes Vitest and adds a redirect test — that test MUST pass as part of this gate. `npm run build && typecheck` alone does NOT cover T009's Constitution VI mandate; the test suite execution is a required gate step. If `apps/tauri-client` does not yet have a test script in its `package.json`, T009's E3 Vitest bootstrap sub-step must complete first. **B2 (zero-test guard)**: If `npm test --workspace=apps/tauri-client` runs 0 tests (i.e., the test runner reports "0 tests passed" or "no tests found"), this gate does NOT pass — T009's Vitest bootstrap sub-step (E3) has not been completed. A zero-test run is indistinguishable from a no-op pass and does not demonstrate that T009's redirect behavior has automated coverage. The gate ONLY clears when `npm test --workspace=apps/tauri-client` reports ≥ 1 passing test. The same zero-test guard applies to `npm test --workspace=packages/ui` (T006b's mocha bootstrap): ≥ 1 passing `SettingsTabs` contract test is required. Reject "build and typecheck pass, no tests found" as a T010 completion state for either workspace. **Note (D1/E1 — Constitution III reminder)**: Phase 3 (US2) has a hard BLOCKING gate: T043 MUST be completed (schema inspection + write-path verification + CI-passing unit test in `tier3-synthesizer.test.ts`) before ANY of T017–T021 begins. Confirm T043 is queued as the first task of Phase 3 before any Phase 3 work is assigned.

**Checkpoint**: AppConfig updated, tab structure in place, build clean. Story phases may begin only after the following named gates are confirmed:

- **E1 (FR-001 nav link)**: T008/C1's navigation verification is complete — a nav entry to the Settings page exists (or was added) and the grep result is documented. A missing settings nav entry is a P1 blocker; it MUST NOT slip to the Phase 9 verification pass.
- **D2 (Vitest bootstrap — CONFIRMED REQUIRED, verified 2026-07-08)**: `apps/tauri-client/` has NO `vitest.config.ts` and NO `test` script in `package.json` (only `dev`/`build`/`tauri`/`typecheck` exist). T009/E3's Vitest bootstrap is therefore a REAL Phase 1 work item, not a conditional: add `vitest.config.ts` (JSDOM environment), `@testing-library/preact`, and a `test` script before the T010 gate can clear. Budget time for dependency-conflict surprises when introducing a new test framework.
- **D3 (T043 schema pre-inspection — ALREADY PERFORMED, 2026-07-08)**: `apps/hub-server/migrations/001_engineer_events.sql` was inspected: `engineer_events` has columns `id, session_id, tier3_type, prompt, response, tools_called, latency_ms, outcome, created_at` — it does NOT have `llm_model` or `llm_base_url`. T043 case (b) is confirmed in advance: a migration adding those columns IS required, plus the `tier3-synthesizer.ts` write-path change and its unit test. Plan Phase 3 with this migration work included; T043 still executes as the Phase 3 blocking gate.

---

## Phase 2: User Story 1 — Audio Device Configuration (Priority: P1) 🎯 MVP

**Goal**: Driver selects mic + speaker, confirms audio routes correctly, settings persist across restart.

**Independent Test**: quickstart.md SC-1.

- [X] T011 [US1] Implement `set_audio_device(device_name: String, device_type: String)` Tauri command in `apps/tauri-client/src-tauri/src/commands.rs`: validate `device_type` is `"input"` or `"output"`; look up named device in `cpal` host device list; return `Err("device not found: {name}")` if not present; send new device name via a new `tokio::sync::watch` channel to the running audio task; persist selection to `AppConfig`.
- [X] T012 [US1] Extend `apps/tauri-client/src-tauri/src/audio/capture.rs`: accept a `device_rx: tokio::sync::watch::Receiver<Option<String>>` parameter in `AudioCapture::new()`; watch for changes in the capture loop; reinitialize the `cpal` input stream to the new device on change. **E2 (FR-005 — mic level meter emission)**: In the capture loop, after each PCM frame is received from `cpal`, compute a normalized RMS level (0.0–1.0) from the PCM samples and emit a `audio:mic-level { level: f32 }` Tauri event at ~10Hz — **A3: throttle by wall-clock timer (~100ms intervals), NOT by frame count**: at common cpal buffer sizes an every-Nth-frame throttle can undershoot the FR-005 10Hz floor (e.g., ~86 callbacks/s ÷ 10 = 8.6Hz); a wall-clock timer guarantees the floor regardless of buffer size. This event is the data source for the frontend level meter wired in T015; without this emission the meter has no data path. **A1 (always-on meter — architecture note)**: the M5 capture stream runs continuously (`stream.play()` + `mem::forget` in `capture.rs` — it is never stopped), so mic-level emission is effectively always-on while an input device is present. The meter therefore indicates device liveness ONLY — it does NOT indicate PTT state and MUST NOT be used to confirm PTT shortcut firing (that is T023/A1's PTT-active indicator). Emission pauses only during the brief stream re-initialization window on device switch; there is no PTT-gated or session-gated stop path under the current architecture.
- [X] T013 [P] [US1] Extend `apps/tauri-client/src-tauri/src/audio/playback.rs` with the same watch channel pattern for output device switching.
- [X] T014 [US1] Handle unavailable saved device on app startup in `apps/tauri-client/src-tauri/src/commands.rs` (within the startup init flow): if `AppConfig.audio_input_device` or `audio_output_device` is set but not in the current `cpal` device list, fall back to system default and emit a `audio:device-unavailable` Tauri event with `{ deviceType: "input" | "output", savedName: string }`. Audio subsystem continues with system default. **U1 (event replay gap — status MUST also be queryable)**: Tauri events are not replayed to late subscribers, and the Audio tab mounts long after startup — a tab opened later would miss init-time events entirely, making US1 scenario 5 unsatisfiable via events alone. Therefore ALSO record each unavailable-device occurrence in managed state (e.g., `Vec<UnavailableDevice { device_type, saved_name }>` behind the existing app-state mutex) and expose it via a small `get_audio_device_status()` Tauri command (or include it in the `get_config()` response envelope — implementer's choice; document which in the code and in `contracts/tauri-commands.md`). Clear a device's entry from this state when a valid device of that type is selected via `set_audio_device()`. The live event covers mid-session unplug only. **C4 (both devices unavailable simultaneously)**: if BOTH `audio_input_device` and `audio_output_device` are unavailable at startup, emit two `audio:device-unavailable` events (one per type) and display a combined warning in the Audio tab: "Both saved audio devices are unavailable — please reselect your microphone and speaker." Handle each device type independently (do not short-circuit on the first failure). **Edge case**: if `cpal` device enumeration returns an empty list entirely (e.g., headless/VM with no audio hardware), emit a distinct `audio:no-devices` Tauri event with `{ deviceType }`, disable the affected audio subsystem gracefully (log a structured `warn`), and display the per-type user-visible message in the Audio tab (exact strings owned by T015/U1: "No audio input devices found" / "No audio output devices found" per FR-002) — do not panic or crash.
- [X] T015 [US1] Wire the Audio tab in `apps/tauri-client/src/pages/Setup.tsx`: on dropdown selection change, call `invoke("set_audio_device", { deviceName, deviceType })` immediately (live device switch); listen for `audio:device-unavailable` events and display "unavailable — using system default" status in the affected dropdown; **U1 (query on mount)**: ALSO query the cached unavailable-device status on tab mount (via `get_audio_device_status()` or the `get_config()` envelope — whichever T014 implemented) and render the same "unavailable" state from the query result — startup-time events fire before the tab exists and are not replayed, so the mount-time display MUST come from the query, with the event subscription covering live unplug thereafter. Device selections are saved via `save_config()` on explicit Save button press. **C2 (FR-003 — output device enumeration)**: On Audio tab mount, call `list_audio_devices({ device_type: "output" })` (the existing M4/M5 command) and populate the output device dropdown with the returned list. This is the same enumeration pattern as for input devices — confirm `list_audio_devices()` accepts a `device_type` parameter (or equivalent) to distinguish input vs. output. If the existing command returns both types together (flat list), filter by type client-side. The output dropdown MUST be populated from this enumeration result, not from a hardcoded list. If the enumeration returns an empty list (no audio output devices), display "No audio output devices found" and disable the output dropdown. **U1 (input side stated explicitly, not by analogy)**: likewise, if INPUT enumeration returns an empty list, display "No audio input devices found" (the exact FR-002 string) and disable the input dropdown. These per-dropdown messages are distinct from T014's backend `audio:no-devices` event handling — T015 owns the display strings for both device types. **C3 (FR-006 — "Test Playback" button)**: Render a "Test Playback" button in the Audio tab that invokes the existing M4 `test_audio_playback()` Tauri command (not a new command). The button plays a short sample through the currently selected output device. The button MUST be wired to `invoke("test_audio_playback")` directly — do NOT require Save to be clicked first; the test should use the currently live device selection (which is applied immediately via `set_audio_device()` on dropdown change). Verify during SC-1 that the M4 test clip is audible (non-silent, approximately 1 second duration). **E2 (FR-005 — mic level meter wiring)**: Subscribe to `audio:mic-level` events (emitted by T012); render a visual level meter (e.g., an animated bar or percentage indicator) that updates in real time from the event payload's `level: f32` value. Clean up the event listener on component unmount. This closes the frontend half of FR-005 — the backend emission is implemented in T012. **A1 (FR-004 / FR-025)**: Audio device persistence here implements both FR-004 (audio-specific) and FR-025 (all settings). No extra implementation is needed — the `save_config()` call covers both. FR-004 exists only for task-assignment clarity; no additional persistence logic is required beyond what FR-025 already mandates.
- [X] T016 [P] [US1] Unit tests for `set_audio_device` command in `apps/tauri-client/src-tauri/src/commands.rs` tests: device-not-found path returns correct `Err`; invalid `device_type` returns `Err("invalid device type")`; valid device sends signal on the watch channel. **E3 (FR-005 — mic level emission test)**: Add a test for `AudioCapture` in `apps/tauri-client/src-tauri/src/audio/capture.rs` tests: inject a synthetic PCM frame (non-silent, known amplitude) into the capture loop via a mock `cpal` stream; assert that an `audio:mic-level` event with a non-zero `level` value is emitted within 200ms. This verifies the emission path from T012 without requiring real audio hardware.

**Checkpoint**: SC-1 passes — select device, mic meter responds, test clip plays from correct output, settings persist. **E2 (SC-001 timing — measure now, not in Phase 9)**: While running SC-1 at this checkpoint, time the first-time flow (Audio tab open → test clip plays through selected speaker) and confirm it completes in under 3 minutes (spec SC-001). If the flow exceeds the budget, fix the UX friction in this phase — do not defer discovery to T040's Phase 9 timing pass.

---

## Phase 3: User Story 2 — Connection Configuration + LLM Fields (Priority: P1)

**Goal**: Driver configures all connection endpoints including LLM; model name change takes effect without restart.

**Independent Test**: quickstart.md SC-2.

**PHASE GATE — Constitution III (BLOCKING)**: T043 MUST be completed and its outcome documented before ANY other task in this phase (T017–T021) begins. This is a hard sequential dependency, not advisory. If T043 reveals a missing schema column, the migration must be applied and verified before T017/T018 write any hub code that uses `model`/`baseUrl`. Do not parallelize T043 with T017–T021. **D3 reminder**: "T043 completed" means BOTH schema inspection AND write-path verification AND a CI-passing unit test in `tier3-synthesizer.test.ts` (see T043/D1). Schema inspection alone does NOT satisfy this gate — the gate is not cleared until the write-path test is green in CI.

- [X] T043 [US2] **Constitution III audit gate (BLOCKING — FR-029)**: Before writing any hub LLM config code, inspect the M5 `engineer_events` Postgres audit table schema (check `apps/hub-server/migrations/` or equivalent). Determine whether `model` and `baseUrl` (or equivalent LLM call metadata) are logged per Tier 3 synthesis event. **D3 (pre-inspection result, 2026-07-08)**: `apps/hub-server/migrations/001_engineer_events.sql` was already inspected during spec analysis — the table has NO `llm_model` or `llm_base_url` columns (only `id, session_id, tier3_type, prompt, response, tools_called, latency_ms, outcome, created_at`). Case (b) below applies: the migration IS required. Re-verify against the live database at T043 execution time in case the schema drifted, then proceed directly to (b). (a) If YES: document the column names in a comment at the top of T017 and in `data-model.md`; no migration needed. (b) If NO: write and apply a migration (`apps/hub-server/migrations/YYYYMMDD_add_llm_audit_fields.sql` or equivalent) that adds `llm_model VARCHAR` and `llm_base_url VARCHAR` to the `engineer_events` table. **D1 (CRITICAL — write path must also be verified)**: Whether columns already exist (case a) or were just added (case b), ALSO verify that `apps/hub-server/src/engineer/tier3-synthesizer.ts` (or equivalent) actually writes `model` and `baseUrl` to every `engineer_events` row at synthesis time. If it does not, update `tier3-synthesizer.ts` to populate these fields before T017/T018 are merged. Add a unit/integration test in `apps/hub-server/tests/unit/engineer/tier3-synthesizer.test.ts` (or equivalent) asserting that a single synthesis call produces an audit log row where `llm_model` equals the model resolved from `hub:config:llm` — a migration without this write-path test does NOT satisfy Constitution III. **C3 (local test run required before closing gate)**: After writing the unit test, run `npm test` (or the equivalent test command for `apps/hub-server`) locally and confirm `tier3-synthesizer.test.ts` passes. Paste the passing test output in the PR description or as a PR comment before any T017–T021 work is merged. T043 is NOT considered complete until this passing test output is recorded — writing the test file is necessary but not sufficient. **I1 (CI wiring is part of this gate — gap verified 2026-07-08)**: `.github/workflows/ci.yml` currently runs NO npm tests (only `cargo test -p iracing-engineer-lib` + the hub build). Add an `npm test -w apps/hub-server` step to `ci.yml` as part of T043, so the write-path test actually executes in CI — a locally-passing test with no CI execution does NOT satisfy FR-029's enforcement ladder branch (a), and T042/C2(a2) depends on this step existing.

- [X] T017 [US2] Extend `save_config()` in `apps/tauri-client/src-tauri/src/commands.rs`: in the existing async Redis sync task (which already writes `hub:config:personality`), also write `hub:config:llm = { "baseUrl": config.llm_base_url, "model": config.llm_model }` (no `apiKey` — stays local per `contracts/hub-llm-config.md`). **C1 (pipeline refactor prerequisite)**: Before adding `hub:config:llm` to the pipe, inspect the M5 `save_config()` implementation: if the existing personality sync uses a plain `redis.set()` call (not `redis::pipe()`), refactor it to use `redis::pipe()` first, then add the `hub:config:llm` SET to the same pipe. This refactor is a required sub-step of T017 — do not skip it even if the existing personality sync appears to work without pipeline semantics. The T017/C1 unit test (which asserts a single pipeline with both keys) CANNOT pass without this refactor if the M5 code uses sequential calls. Only write if `llm_base_url` or `llm_model` differ from the values last persisted to disk (the values read from the saved config file at startup or at the last successful save — not the in-memory form state) to avoid spurious Redis writes. **I4 (retry cross-reference)**: T017 does NOT implement retry logic — on pipeline failure, surface `HubSyncError::RedisWriteFailed(reason)` and return. Retry behavior (on the next explicit Save click) is defined in T039; do NOT add automatic retry in T017. **I2 (disk-first ordering — FR-027)**: The Redis hub sync pipeline MUST NOT be attempted until local disk save has returned `Ok(())`. If local disk save fails, return `Err` immediately without touching Redis — this enforces FR-027's requirement that hub sync is only attempted after a successful local save. **F1 (pipeline, not sequential)**: Write both `hub:config:personality` and `hub:config:llm` in a single Redis pipeline using `redis::pipe().set(...).set(...).query_async(&mut con).await?` — this batches both SET commands in one TCP round-trip and ensures they are either both queued or the entire pipeline fails. This satisfies FR-027's best-effort atomicity for hub sync (see FR-027 for the definition of "pipeline-batched" in this context). If the pipeline fails, surface a single `HubSyncError::RedisWriteFailed(reason)` — do not let errors pass silently. **C1 (pipeline-batching unit test)**: Add a unit test in the Rust test suite for `commands.rs` that mocks the Redis client and asserts that a single `save_config()` call issues exactly ONE pipeline (i.e., exactly one `redis::pipe()` invocation) that includes both a `SET hub:config:personality` command AND a `SET hub:config:llm` command in the same pipe object — NOT two sequential `redis.set()` calls. The test MUST fail if the implementation switches from pipeline to sequential writes. **I2 (fixture precondition — required for the test to be valid)**: this test's fixture MUST use LLM values that DIFFER from the last-persisted values — otherwise the differ-guard above legitimately omits the `hub:config:llm` SET and the test would fail against correct code. Add a companion assertion for the unchanged case: a `save_config()` call whose LLM fields match the last-persisted values issues one pipeline containing the `hub:config:personality` SET and NO `hub:config:llm` SET (the differ-guard working as designed). **C2 (Constitution V — API key observability)**: When `llm_api_key` changes (differs from the previously persisted value), emit a structured audit log entry `{ event: "llm-api-key-updated", hasKey: !new_key.is_empty() }` — NEVER log the key value itself. This satisfies the observability requirement for sensitive credential changes without exposing the key in logs. **D2 (test ownership)**: The unit test for this audit event belongs in the Rust test suite for `commands.rs` — test that a `save_config()` call with a changed `llm_api_key` emits exactly one `{ event: "llm-api-key-updated", hasKey: bool }` log entry with no key value in the output; assert the converse (no event when key unchanged). NOT in T021 (hub TypeScript tests). **U2 (PR documentation step — FR-027/U2, I6)**: Include in this task's PR description the documented limitation that there is NO automatic startup resync after a failed hub sync — if a Save succeeds locally but the Redis pipeline fails, the hub reads possibly-stale Redis values until the driver's next explicit Save (see FR-027/U2 and quickstart SC-6b). This PR note is a required FR-027/U2 deliverable, not optional.
- [X] T018 [US2] Extend `apps/hub-server/src/engineer/llm-client.ts`: at the top of each LLM synthesis call, `await redis.get("hub:config:llm")` → JSON parse → apply `baseUrl` and `model`; if key absent, fall back to `engineer-config.json` `llm` defaults and emit one `warn` structured log per startup: `{ event: "llm-config-fallback", reason: "hub:config:llm absent" }`; if malformed JSON, same fallback + `{ event: "llm-config-malformed" }`. Mirrors the `parsePersonality()` pattern in `personality-config.ts`. **U1 (degraded-mode behavior for invalid model name)**: If the model resolved from `hub:config:llm` causes a non-2xx response from the LLM endpoint during Tier 3 synthesis (e.g., HTTP 404 from a model name typo), the hub MUST: (a) emit a structured `warn` log `{ event: "llm-synthesis-failed", model, reason, statusCode }`, (b) suppress the Tier 3 output entirely (silence — per Constitution I degraded-mode guidance), and (c) NOT automatically fall back to the previous model or silently retry. The driver must update the model name via the UI to restore Tier 3 synthesis. Do not crash, do not emit stale output.
- [X] T019 [P] [US2] Add `check_llm(base_url: String, model: String, api_key: String)` Tauri command in `apps/tauri-client/src-tauri/src/commands.rs`: make a minimal HTTP probe to `GET {base_url}/models` ONLY with 5s timeout (**A1 — the earlier "(or a 1-token completion)" option is REMOVED**: a completions-endpoint call is an inference call regardless of token count and sits exactly on the Constitution V audit-gate boundary that FR-008/D3 exempts this command from); return `ConnectionTestResult { service: "llm", ok, latencyMs, error }` per `contracts/tauri-commands.md`. Auth header included only if `api_key` is non-empty. **A1 (error taxonomy)**: the `error` field in `ConnectionTestResult` MUST use one of these specific strings so the UI can display actionable messages: `"connection refused"` (TCP failed), `"timeout after 5s"` (no response), `"HTTP 401 — check API key"` (auth failure), `"HTTP 404 — verify base URL and model path"`, `"HTTP {status} — unexpected response"` (other HTTP errors), `"invalid URL"` (malformed base_url). Add these strings to `contracts/tauri-commands.md` as the `check_llm` error enum. **D2 (contract-first gate) — PRE-APPLIED 2026-07-08**: the `check_llm` error enum has already been written to `contracts/tauri-commands.md` during analysis remediation. T019's first step is now to VERIFY the contract enum matches the A1 list above (reconcile both together if either has drifted) before implementing the command. T020 depends on these strings being in the contract file (it displays them); T020 MUST NOT begin until this verification is complete. **Constitution V**: on any `Err` path, emit a structured `warn` log entry `{ event: "llm-connectivity-check-failed", reason: err }` before returning — no silent failures. **D3 (synthesis prohibition — Constitution V guard)**: `check_llm()` MUST probe only the non-inference endpoint `GET {base_url}/models` (A1 — the 1-token-completion alternative was removed; any completions/inference-endpoint call counts as synthesis for audit purposes regardless of token count and voids this command's audit-gate exemption). If a synthesis call is ever added to this command in the future, Constitution V Principle V's audit gate applies and an `engineer_events` row must be written. Add a comment to this command in the code: `// MUST NOT call synthesis endpoint — audit gate exemption depends on this`. **G2 (unit tests — plan DoD requires them; previously unowned)**: Add Rust unit tests for `check_llm` in the `commands.rs` test suite (mock at the HTTP client layer or spawn a local test listener — implementer's choice): one case per A1 error-taxonomy string (connection refused, timeout, 401, 404, other HTTP status, invalid URL) asserting the exact `error` string and `ok: false`; a success case asserting `ok: true` with `latencyMs` populated; and an assertion that no `Authorization` header is sent when `api_key` is `""` (data-model.md's empty-key sentinel rule). These run under `cargo test` (T041 gate).
- [X] T020 [US2] **Prerequisite: T019 must have updated `contracts/tauri-commands.md` with the `check_llm` error enum before this task begins.** Add LLM fields to the Connection tab in `apps/tauri-client/src/pages/Setup.tsx`: LLM API Base URL (text input), LLM Model (text input), LLM API Key (password/masked input). Add "Test" button for each of the three services (Redis, Hub, LLM) that call their respective check commands and display a green ✓ / red ✗ with the error message. Add URL format validation inline — disable Save until all URL fields are valid (FR-026). **A4 (empty API key is valid — FR-007 exception)**: The LLM API key field MUST accept empty string as valid — inline validation MUST NOT flag or block Save when the API key field is blank. **G1 (blank model name blocks Save — FR-009/FR-026)**: The LLM Model Name field MUST have a non-empty inline validation: Save is disabled with an inline error while `llm_model` is blank (whitespace-only counts as blank). Full validation matrix: URL-format checks on `llm_base_url`, `redis_url`, `hub_url`; non-empty check on `llm_model`; no validation on the optional API key. **F1 (first-launch LLM hint)**: On first launch (detected via `AppConfig.first_launch_seen == false`, added in T001), display a dismissable annotation adjacent to the LLM Base URL and Model Name fields: "Default — update for your setup." **I1 (dismiss is session-only — NOT persistent)**: The dismiss action on the hint (e.g., clicking an "×" close button) hides the hint for the current app session only — it does NOT set `first_launch_seen = true` and does NOT persist to disk. Persistent suppression occurs ONLY when `save_config()` succeeds: then set `first_launch_seen = true` (via the standard save call — no extra write needed). If `save_config()` returns `Err`, `first_launch_seen` MUST remain `false` — the hint reappears on the next launch. Add a comment: `// Dismissing the hint hides it for this session only; first_launch_seen is set on successful Save.` This implements the FR-007/C1 requirement. **E2 (layout assertion per FR-008)**: Confirm all three Test buttons are rendered with distinct labels matching FR-008 ("Test Redis", "Test Hub", "Test LLM" or equivalent); add this assertion to T040's end-to-end verification step or as a visual check in quickstart.md SC-2. **Edge case coverage**: ensure `check_hub()` distinguishes between "TCP connection refused" and "TCP connect OK but HTTP error response" (e.g., hub process not started vs. hub returning 503) — these should produce different error messages in the UI. Verify this behavior is covered by the existing M4 `check_hub()` implementation or add a test for it. **E1 (cloud URL pattern warning — FR-007 deliverable)**: When the LLM Base URL field value matches a known cloud provider pattern, display a visible warning adjacent to the field: "API key is stored locally but not forwarded to the hub server — Tier 3 synthesis will fail if this endpoint requires authentication." The cloud hostname patterns MUST be defined as a named constant array (not inline string comparisons) — e.g., `const CLOUD_LLM_HOSTNAMES = ['api.anthropic.com', 'api.openai.com', 'generativelanguage.googleapis.com', 'openrouter.ai']` — in `apps/tauri-client/src/constants.ts` (or equivalent constants file). Check on every URL field change event (not just on blur or Save). This warning is NOT a Save blocker — it is informational only. **B3 (constants in one place)**: The same hostname list MUST be used for the warning check and for any future API key forwarding logic — define it once and import it wherever needed. Do not duplicate the list.
- [X] T021 [P] [US2] Unit tests for `llm-client.ts` Redis read in `apps/hub-server/tests/unit/engineer/llm-client.test.ts` (mocked `ioredis`): absent key → config defaults used + exactly one `warn` log emitted; present key with valid JSON → `baseUrl`/`model` applied on next call; malformed JSON → config defaults + one `warn` log; key changes between calls → new values applied without restart. **C3 (no-restart constraint)**: Add a specific test sequence that proves FR-009: call `synthesize()` with `hub:config:llm` = `{ model: "model-A" }` → update mock Redis to return `{ model: "model-B" }` → call `synthesize()` again → assert the second call used `"model-B"` — all without simulating a process restart. This directly validates the "no hub restart required" constraint from SC-002. **C2**: Include an assertion that the mock Redis value under test never contains an `apiKey` field, and that the llm-client does not read or expose `apiKey` from the Redis key at any point (confirming the write contract from T017 is enforced end-to-end per `contracts/hub-llm-config.md`). **I2 (D3 scope correction — audit row assertion belongs in T043)**: T021 tests `llm-client.ts` and cannot easily assert a Postgres row written by `tier3-synthesizer.ts` without a deep integration setup. T021 should assert only that the `model` value resolved from `hub:config:llm` is passed as the correct argument to the downstream `synthesize()` call — NOT that it arrives in `engineer_events`. The Postgres audit row assertion is already specified in T043/D1 and MUST live in `tier3-synthesizer.test.ts`. **Note on D2 (API key audit event test)**: The `{ event: "llm-api-key-updated" }` audit log test belongs in the Rust test suite for `commands.rs` (testing `save_config()`), not here in the hub TypeScript tests. See T017 (C2) for the test specification. **E1 (in-flight model switch test)**: Add a test case to the no-restart sequence that asserts: if a `synthesize()` call is awaiting when `hub:config:llm` is updated in Redis, the in-flight call MUST complete with the model resolved at its start — it does NOT pick up the new model mid-execution. Sequence: start a slow mock synthesis (delayed completion), update mock Redis to return `model-B` mid-await, confirm the first call completes with `model-A`, then assert the next call uses `model-B`. **G3 (degraded-mode case — T018/U1 behavior)**: Add a test case: mock the LLM endpoint returning a non-2xx (e.g., HTTP 404 from a model-name typo) during synthesis → assert (a) exactly one structured `warn` log `{ event: "llm-synthesis-failed", model, reason, statusCode }` is emitted, (b) the Tier 3 output is suppressed entirely (no output delivered), and (c) NO automatic fallback to a previous model and NO silent retry occurs — the next call still uses the configured (broken) model until the driver changes it. This is the T018/U1 degraded-mode contract; without this case the LLM delivery-path behavior change has no test.

**Checkpoint**: SC-2 passes — change model name → hub log shows new model on next Tier 3 with no restart.

---

## Phase 4: User Story 3 — PTT Hotkey Interactive Binding (Priority: P2)

**Goal**: Driver clicks "Set PTT Key," presses a key, binding is registered globally and displayed.

**Independent Test**: quickstart.md SC-3 and SC-3b.

- [X] T022 [US3] Implement `bind_ptt_hotkey()` Tauri command (FR-010, FR-011) in `apps/tauri-client/src-tauri/src/commands.rs` + extend `apps/tauri-client/src-tauri/src/hotkeys/ptt.rs`: enter "listening" mode via `tauri-plugin-global-shortcut` (unregister existing PTT binding; register a one-shot catch-all); await first non-modifier keypress with `tokio::time::timeout(Duration::from_secs(10))`; on capture, unregister catch-all, call `app.global_shortcut().register(captured_key, ptt_handler)`; on success, persist to `AppConfig.ptt_hotkey` and return `Ok(key_name: String)`; on timeout return `Err("ptt:timeout")`; on OS conflict return `Err("ptt:key-conflict")`; on accessibility permission denied (macOS) return `Err("ptt:accessibility-denied")`. **I1 (contract alignment — updated 2026-07-08)**: `contracts/tauri-commands.md`'s `bind_ptt_hotkey` section now lists these same four structured codes (`ptt:timeout`, `ptt:key-conflict`, `ptt:accessibility-denied`, `ptt:capture-in-progress`) — it previously specified human-readable strings; verify the implementation matches the contract and keep T022/T023/T024 and the contract in sync if codes ever change. **C5 (PTT auto-save on success)**: On `Ok(key_name)` return, `bind_ptt_hotkey()` MUST call `save_config()` immediately before returning — do NOT wait for the driver to click the Save button. PTT bindings take effect at the OS level the moment they are registered; if the driver restarts the app before clicking Save, the OS binding is re-registered from `AppConfig.ptt_hotkey` at startup. A PTT key captured but not yet saved would be re-registered on the next restart as the previous (stale) binding. Therefore, `bind_ptt_hotkey()` auto-saves `AppConfig.ptt_hotkey` on success, independent of the form's Save button. The form's Save button continues to save all other form fields (LLM URL, Redis URL, etc.) but does NOT need to additionally save `ptt_hotkey` — it is already persisted. **A2 (hint lifecycle isolation)**: this auto-save persists `ptt_hotkey` (and the otherwise-unchanged config) but MUST NOT set `first_launch_seen = true` — the first-launch LLM hint (FR-007) is suppressed only by an explicit Save-button save from the settings form (T020/I1, where the FRONTEND sets the flag in the save payload); a driver who binds PTT before ever visiting the Connection tab keeps the hint. **U2 (auto-save failure semantics)**: if the global shortcut registers successfully but the C5 auto-save fails (disk error), do NOT unregister the binding — it remains active for the current session. Still return `Ok(key_name)`, emit a structured warn `{ event: "ptt-save-failed", reason }`, and emit a `ptt:save-failed` Tauri event; T023 maps it to a dismissable warning ("PTT key bound for this session, but saving failed: [reason] — the binding will revert on restart"). The FR-012 persistent banner is NOT used (the binding itself succeeded); persistence recovers on the next explicit Save, which writes the full config including `ptt_hotkey`. **U2 (mid-session rebind behavior)**: If `bind_ptt_hotkey()` is invoked while a PTT capture is already in progress (i.e., the PTT key is physically held down when the rebind flow starts), the new rebind MUST NOT interrupt the current capture — the rebind attempt is REFUSED and the active capture completes normally to its key-release event; the driver re-initiates the rebind afterward (I5 — an earlier draft said the rebind "takes effect for subsequent keypresses," implying an automatic deferred rebind; there is no deferred rebind — the attempt is refused outright). Implement this by checking `ptt_capture_active` state before entering the rebind listening mode: if active, return `Err("ptt:capture-in-progress")` immediately, and have T023 map that code to "PTT is currently active — release the key first, then rebind." **C1 / FR-028**: The command returns structured error codes (not human strings) so T023 can map each code to the appropriate UI message and actions (e.g., `ptt:accessibility-denied` → "Accessibility permission required — open System Preferences → Privacy & Security → Accessibility" + an "Open Settings" button). This keeps the command testable and the UI message centralized in the frontend. **Constitution V (D2)**: emit a structured `warn` log `{ event: "ptt-bind-failed", reason }` on every `Err` path before returning.
- [X] T023 [US3] Wire the Hotkeys tab in `apps/tauri-client/src/pages/Setup.tsx`: display current `ptt_hotkey` value from config. **F4 (never-configured vs. failed-binding state)**: If `ptt_hotkey == ""` (the M10 default — never configured; T001/I1 changes M5's `"F13"` default to `""` so this state is reachable on fresh installs, while M5-upgraded configs retain their saved key), display a soft dismissable prompt: "No PTT key set — click 'Set PTT Key' to bind one." This prompt IS dismissable (clicking its dismiss control hides it until next mount) and is NOT the FR-012 persistent banner. If `ptt_hotkey != ""`, display the current key name instead. The FR-012 persistent non-dismissable error banner is shown only after a banner-qualifying failed binding attempt per FR-012/A1's code partition: `ptt:accessibility-denied` always qualifies (registration is impossible, so the old key's restore fails too); `ptt:key-conflict` qualifies only when NO previous binding exists (prior `ptt_hotkey == ""`); a `ptt:key-conflict` rebind where a previous working key was restored gets a dismissable warning "Couldn't bind the new key: [specific error] — keeping [previous key]" instead (voice queries still work — "Voice queries disabled" would be false); `ptt:timeout` and `ptt:capture-in-progress` are transient interaction outcomes and get inline transient messages only, NEVER the banner. These states MUST be rendered differently in the component. "Set PTT Key" button → invoke `bind_ptt_hotkey()` → show "Listening for key..." indicator while awaiting; on `Ok(keyName)` update display to new key name; on `Err(code)` map the structured error code to a user-facing message: `ptt:timeout` → "No key pressed — try again"; `ptt:key-conflict` → "This key is already bound by the OS — try a different key"; `ptt:accessibility-denied` → "Accessibility permission required — open System Preferences → Privacy & Security → Accessibility" with a prominent "Open Accessibility Settings" button that calls `open::that("x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")` via Tauri shell open (FR-012, FR-028); include a brief note about Stream Deck passthrough (maps buttons to keypresses — no bespoke integration needed). **A1 (PTT-active indicator — the US3-2/SC-003 confirmation signal)**: Render a PTT-active indicator in the Hotkeys tab (e.g., a dot/badge labeled "PTT" that lights while the key is held). Data source: the M5 PTT press/release handling in `hotkeys/ptt.rs` — if M5 already emits frontend-visible press/release Tauri events, subscribe to those; if it does not, add a minimal `ptt:state { active: bool }` Tauri event emission in the press/release handler as part of this task (a small `ptt.rs` addition is in scope here). Set the indicator active on press, inactive on release; clean up listeners on unmount. This indicator — NOT the mic level meter — is the confirmation signal for US3 scenario 2, spec SC-003, and quickstart SC-3 step 7: the mic meter is always-on (see T012/A1) and cannot confirm shortcut firing. **I5 (button disabled during listening)**: During the listening state (after "Set PTT Key" is clicked, before a key is captured or the 10-second timeout fires), the "Set PTT Key" button MUST be disabled to prevent re-entry. The button re-enables when `bind_ptt_hotkey()` returns `Ok`, returns `Err`, or the 10-second timeout fires (the `Err("ptt:timeout")` case). Do not allow concurrent listen sessions. **E1 (banner persistence across tab navigation — FR-012)**: The PTT error banner MUST be stored in persistent component state (e.g., a `useRef` or module-level signal — NOT derived from the most recent `bind_ptt_hotkey()` call). On mount, initialize the banner state from the last known error (if any — persist the last error code in a module-level ref that survives tab unmount/remount). The banner is cleared ONLY when `bind_ptt_hotkey()` returns `Ok` — navigating away from Hotkeys and back MUST still show the banner from the previous failed binding attempt. Add a Preact unit test: simulate `ptt:accessibility-denied` error → navigate away → navigate back → assert banner is still visible. **A1 (second case — restored-binding split)**: add a second Preact case: simulate a `ptt:key-conflict` rebind failure with a previous working binding present (prior `ptt_hotkey != ""`) → assert the dismissable "Couldn't bind the new key … keeping [previous key]" warning renders and the persistent FR-012 banner does NOT; and a `ptt:timeout` → assert only the inline transient message, no banner. **C5 (Windows fallback for accessibility-denied)**: On Windows, `ptt:accessibility-denied` is unlikely (Windows does not use the same Accessibility permission model as macOS) but if `register()` returns a permission error on Windows, map the code to a generic message: "Could not register the PTT key — the OS rejected the binding. Try a different key or run the app as administrator." Do NOT show the macOS "Open Accessibility Settings" button on Windows. **U2 (auto-save failure warning)**: subscribe to the `ptt:save-failed` Tauri event (T022/U2) and render a dismissable warning — "PTT key bound for this session, but saving failed: [reason] — the binding will revert on restart" — distinct from both the never-configured prompt and the FR-012 persistent banner (the binding itself succeeded; only persistence failed).
- [X] T024 [P] [US3] Unit tests for `bind_ptt_hotkey` in `apps/tauri-client/src-tauri/src/` tests: timeout path returns `Err("ptt:timeout")`; OS-conflict path returns `Err("ptt:key-conflict")`; accessibility-denied mock (inject error from `register()`) returns `Err("ptt:accessibility-denied")`; confirm existing PTT binding is restored (not lost) if new binding fails. **A1 (pairing note)**: the restored-binding rebind-failure case pairs with T023's dismissable "keeping [previous key]" warning, NOT the FR-012 banner — this Rust test asserts the restoration; the corresponding UI split is tested in T023/E1's second Preact case. **D2**: Each `Err` path MUST also assert that exactly one structured `warn` log `{ event: "ptt-bind-failed", reason }` was emitted (use a log capture spy or test logger) — the Constitution V requirement from T022 is not tested without this assertion. **E2 (URI constant)**: Define the macOS Accessibility deep-link URI as a compile-time constant `const MACOS_ACCESSIBILITY_PREFS_URI: &str = "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility"` in `hotkeys/ptt.rs` and add a unit test asserting the constant has not been accidentally changed — a string typo in this URI silently breaks the "Open Settings" button (FR-028).

**Checkpoint**: SC-3 passes — bind F14, hold it, the PTT-active indicator in the Hotkeys tab lights (I2 — the mic meter is NOT the confirmation signal; it is always-on per T012/A1 and T023/A1). SC-3b: accessibility denied → correct error message shown.

---

## Phase 5: User Story 4 — Engineer Personality Configuration (Priority: P2)

**Goal**: All 5 personality sliders display current values, save to hub without restart, Energy=1 shows suppression warning.

**Independent Test**: quickstart.md SC-4.

- [X] T025 [US4] Verify the Personality tab (FR-013; SC-004) in `apps/tauri-client/src/pages/Setup.tsx` correctly initializes `PersonalityPanel` with the current `formState` values from `get_config()` — all 5 sliders must show saved values (not defaults) when the tab is opened. **F4 (formState initialization)**: `formState` is initialized from `get_config()` (disk values) when the Settings page first mounts; T025 verifies this initialization path specifically — not that the slider reflects whatever `formState` happens to hold at an arbitrary moment, but that `formState` was correctly seeded from disk on mount. If `formState` can diverge from saved config (unsaved edits in another tab), this task confirms the Personality tab reads from the disk-initialized value on mount, not a potentially stale in-memory form state. Confirm the Energy=1 suppression warning text is rendered (currently may be handled inside `PersonalityPanel` — verify it is visible to the driver in the tab context). **E2 (explicit pass/fail criterion)**: Pass = the text "Tier 2 and Tier 3 commentary will be suppressed" (or equivalent wording per FR-014) is visible in the Personality tab when the Energy slider is set to 1, without requiring Save to be clicked. Fail = warning text absent when Energy=1. If `PersonalityPanel` does not already render this text on Energy=1, it MUST be added in this task — this is a mandatory FR-014 requirement, not optional. **FR-026 coverage (F3 — split test ownership, C1)**: two assertions in two suites, because `save_config()` is a Rust Tauri command and a hub TypeScript test cannot assert Rust behavior: (a) **hub side** — add an assertion to `apps/hub-server/tests/unit/engineer/personality-config.test.ts` that out-of-range trait values read from Redis (e.g., `energy: 0` or `energy: 6`) are clamped to 1–5 on load in `personality-config.ts`; (b) **Rust side** — add a test to the `commands.rs` Rust suite that a `save_config()` call carrying an out-of-range trait value never persists or pipelines a value outside 1–5 (clamp or reject; assert the written value is in range). Together these close the slider range validation gap in FR-026.
- [X] T026 [US4] **Note (E3 / D2 — dependency on T025)**: T026 is NOT parallelizable with T025 regardless of whether T025 requires rendering changes — BOTH tasks write assertions to `personality-config.test.ts` (T025/F3 adds clamping assertions; T026 adds `energy: 1` Redis write assertion) and running them concurrently risks merge conflicts on the same test file. T026 MUST start after T025 completes. If T025 confirms `PersonalityPanel` already renders the Energy=1 warning without modification AND T025's `personality-config.test.ts` assertion is committed, only then may T026 begin. Confirm `save_config()` personality Redis sync (FR-015) still works correctly from within the new tab structure (regression check): save personality traits → verify `hub:config:personality` is updated in Redis → verify next engineer message uses updated traits. This is primarily a manual check (SC-4 scenario) but add an assertion to the existing `personality-config.test.ts` that a `save_config()` call with `energy: 1` results in the Redis key reflecting `energy: 1`.

**Checkpoint**: SC-4 passes — sliders show correct values, Energy=1 suppresses Tier 2, change takes effect without restart.

---

## Phase 6: User Story 5 — Telemetry Debug Readout (Priority: P2)

**Goal**: Debug tab shows live telemetry variables (≥1Hz update), stream lag warning fires at 500ms threshold.

**Independent Test**: quickstart.md SC-5.

- [X] T027 [US5] Implement `get_debug_snapshot()` Tauri command in `apps/tauri-client/src-tauri/src/commands.rs`: return the current `DebugSnapshot` struct (see `data-model.md`) — reads from in-memory state; returns `{ sessionActive: false, ... }` when no session is active. Register command in `lib.rs`.
- [X] T028 [US5] Wire the Rust telemetry subscription loop (in `apps/tauri-client/src-tauri/src/`) to emit `telemetry:debug-snapshot` Tauri events at ~1Hz while a session is active: once per second, collect `DebugSnapshot` fields (fuelRemaining, currentLap, trackPosition, lapTimeDelta from the latest telemetry frame; redisStreamLagMs from stream consumer metrics; redisConnected from connection state; whisperModelLoaded from STT init state; hubConnected derived from a lightweight HTTP health probe fired in the same loop — hub is "connected" if last successful probe was within 10 seconds, "disconnected" otherwise per FR-016) and emit via `app_handle.emit("telemetry:debug-snapshot", snapshot)`. The hub probe MUST be non-blocking and fire async alongside the snapshot collection — do not await it on the snapshot critical path; use a cached `last_hub_probe_ok` timestamp instead. **B1 (probe timeout required)**: The hub HTTP health probe MUST have an explicit timeout of 2 seconds (`timeout(Duration::from_secs(2), probe_future)`) — without this, a slow hub response could block the probe task indefinitely, causing the cached timestamp to go stale and the 10-second window to expire silently (marking hub as disconnected when it is not). If the probe times out, treat it as a probe failure (do NOT update `last_hub_probe_ok`). **A1b (probe coalescing — single-flight guard)**: The probe task has a 2-second timeout but fires at 1Hz — meaning a hung probe could overlap the next probe spawn. Prevent concurrent probes with a single-flight guard: track `probe_in_flight: AtomicBool`; skip the new probe spawn if the previous one has not resolved yet; only update `last_hub_probe_ok` when a completed probe returns `Ok`. This prevents task accumulation under backpressure. **I3 (guard release in all paths)**: The single-flight guard MUST be released in BOTH success AND failure (including timeout) return paths from the probe task — use a `defer` pattern or RAII guard (`scopeguard::defer!`) to guarantee `probe_in_flight.store(false, Relaxed)` runs on every return path, including the timeout error path. An implementation that fails to release the guard on timeout will stall all future probes indefinitely (the guard stays `true` forever after the first timeout). **B1 (stale threshold semantics — wall-clock from last successful RESULT)**: The 10-second stale threshold in `last_hub_probe_ok` is measured from the last probe that returned `Ok` (a successful HTTP response) — NOT from the last probe ATTEMPT. Under backpressure (probe takes >1s), the single-flight guard skips probe ATTEMPTS but does NOT affect the stale countdown: if a slow probe eventually returns `Ok`, it updates `last_hub_probe_ok` and the 10-second window resets. The hub is only marked "disconnected" when 10 seconds have elapsed since the last SUCCESSFUL response — probes that were skipped by the single-flight guard are irrelevant to this count. Document this in a code comment adjacent to `last_hub_probe_ok`. Stop emitting when session ends; emit one final `{ sessionActive: false }` event. **F2 (probe rate under backpressure — worst-case disconnect timing)**: The spec's "1Hz ± 200ms" probe rate is the nominal rate. Under the single-flight guard with a 2s timeout, the effective probe rate may drop to ~0.5Hz if every probe takes the full 2s before timing out. In that case, the 10-second stale window (measured from last successful probe) may not detect a hub failure for up to 10s AFTER the last probe returned `Ok` — meaning worst-case total detection lag (from when hub goes down to when `hubConnected` shows `false`) could be ~12s (last 2s probe + 10s stale window). Document this in a code comment adjacent to `last_hub_probe_ok`: "Worst-case hub disconnect detection: up to [probe timeout] + [stale window] = ~12s after hub becomes unavailable. US5 scenario 4's '10 second' criterion is measured from last successful probe, not from hub failure onset." **A3 (initial hubConnected state)**: On first snapshot emission (before any probe has completed), emit `hubConnected: null` (Rust `None` → JSON `null` — see `data-model.md` `DebugSnapshot` which declares `hubConnected: boolean | null` as the wire type). The frontend maps `null → "unknown"` display string (neutral/grey indicator — T029 MUST do this mapping explicitly: `null → "unknown"`, `true → "Connected"`, `false → "Disconnected"`). Do NOT emit the string `"unknown"` from Rust — only `null` crosses the Tauri IPC boundary. Transition to `true`/`false` after the first successful or failed probe result. **C3 (whisperModelLoaded prerequisite — hard gate before T028)**: Before implementing T028, FIRST verify whether the M5 Whisper init path exposes a readable status signal (e.g., an `Arc<AtomicBool>` set at end of model loading in `stt/whisper.rs` or equivalent). **A1 (this verification is blocking)**: T028 MUST NOT be implemented until this check is done and its outcome documented. Two paths: (a) Signal exists → read it in the snapshot loop (read-only, no M5 code changes); document the actual signal name and type in a comment in `data-model.md` `DebugSnapshot`. (b) Signal does NOT exist → add an `Arc<AtomicBool>` status flag to `WhisperState` in M5 source and wire it in the init path as a prerequisite before T028 begins; if adding the flag to M5 is blocked, use the time-based fallback (5s after startup → `true`, emit one-time `warn` log `{ event: "whisper-status-signal-not-found", fallback: "assumed-loaded-after-5s" }`) and formally note in the PR that the Whisper status field in FR-016 is approximated in M10. **I3 (whisperModelLoaded source)**: `whisperModelLoaded` is set by the M5 Whisper STT initialization path — locate the existing M5 signal (likely an `Arc<AtomicBool>` or a channel set at the end of model loading in `stt/whisper.rs` or equivalent) and read it in the snapshot loop. Do not re-initialize or modify the M5 STT code — read-only access to the existing init state flag only. **C2 (tri-state — "Load failed" requires a third value)**: US5 scenario 5 requires three display states: loading/pending, "Ready," and "Load failed." A single `Arc<AtomicBool>` can only represent two states. To support "Load failed," use `Option<bool>` in the snapshot struct instead: `None` = loading/pending, `Some(true)` = ready, `Some(false)` = load failed. Update the `DebugSnapshot` struct and `data-model.md` `DebugSnapshot` to reflect `whisperModelLoaded: boolean | null` (wire type, analogous to `hubConnected`), where the frontend maps `null → "Loading…"`, `true → "Ready"`, `false → "Load failed."` If the M5 Whisper init path cannot signal failure separately from non-completion, use `null` for both and treat the time-based fallback (defined in T028/C3 path (b) — I6: an earlier draft cited a nonexistent "T028/U3" label) as "Ready after 5s" — but document this limitation in the PR.
- [X] T029 [US5] Wire the Debug tab in `apps/tauri-client/src/pages/Setup.tsx`: call `get_debug_snapshot()` on mount for initial values; subscribe to `telemetry:debug-snapshot` events (cleanup on unmount); display the 4 fixed telemetry variables + infrastructure status badges (Redis, Hub, Whisper); show "No active session" state when `sessionActive: false`; highlight `redisStreamLagMs` in red when > 500ms with a visible warning label (FR-018). **A3 (hubConnected initial state — I4 wire-type correction)**: Render `hubConnected === null` (the actual wire value — Rust `None` serializes to JSON `null`; the string `"unknown"` is a display concept and NEVER arrives on the wire, per data-model.md B3) as a neutral grey indicator (e.g., "Checking…") — NOT as red "Disconnected." Use data-model.md's mapping expression: `const display = snapshot.hubConnected === null ? "unknown" : snapshot.hubConnected ? "Connected" : "Disconnected";`. Transition to green "Connected" or red "Disconnected" after the first probe completes. **B2/E1 (SC-005 hysteresis — false positive prevention and dismissal coverage)**: The lag warning MUST NOT clear immediately on the first sub-500ms snapshot — implement a **time-based** 5-second hysteresis using wall-clock time (not event count, to handle irregular snapshot delivery): record `lagClearStartedAt = Date.now()` when lag first drops to ≤500ms; only dismiss the warning when `Date.now() - lagClearStartedAt >= 5000`. Any >500ms snapshot before the 5000ms threshold resets `lagClearStartedAt = null`. The tab-open initial state MUST have no warning visible regardless of the first snapshot's lag value — the warning may only appear after at least one event cycle is received and the 500ms threshold is crossed. **E1 (dismissal-side test — required)**: In addition to the false-positive guard test (first event does NOT show warning), add a Preact unit test using mocked `Date.now()` that covers the full dismiss path: (1) simulate sustained >500ms lag for N snapshots → assert warning IS showing; (2) simulate 5 continuous seconds of ≤500ms lag (mocked timestamps) → assert warning IS DISMISSED. This closes the gap where only the no-false-positive path is tested but the clearing path has no automated coverage. **B2 (explicit initial mount state)**: The lag warning state MUST be initialized as `{ lagWarningVisible: false, lagClearStartedAt: null, hasReceivedFirstSnapshot: false }`. The `> 500ms` check MUST NOT execute until `hasReceivedFirstSnapshot === true` — events received before this flag is set are ignored for lag warning purposes. Set `hasReceivedFirstSnapshot = true` on receipt of the very first `telemetry:debug-snapshot` event. Add this invariant to the mocked `Date.now()` unit test: assert that a `> 500ms` snapshot as the FIRST event does NOT trigger the lag warning.

- [X] T029b [P] [US5] Rust unit tests for the Debug tab backend (**G1 — Constitution VI**; plan Phase E gate names these; no Rust test task existed in this phase): in the `commands.rs` / telemetry-loop test suites, add: (1) `DebugSnapshot` serialization round-trip — construct a fully-populated snapshot (including `hubConnected: None` and `whisperModelLoaded: None`), serialize to JSON, assert wire types match `data-model.md` (`None` → JSON `null`, never omitted and never the string `"unknown"`), deserialize back, assert equality; (2) `get_debug_snapshot()` with no active session returns `{ sessionActive: false, ... }` with all telemetry fields `null`; (3) single-flight guard release on timeout (T028/I3) — simulate a probe future that exceeds the 2s timeout and assert `probe_in_flight` is `false` after the timeout path returns (i.e., a subsequent probe attempt is not blocked); a guard leak here stalls all future probes permanently, which is exactly the failure mode T028/I3 warns about.

**Checkpoint**: SC-5 passes — live values update ≥1Hz, lag warning fires at threshold, no-session state shown correctly. T029b unit tests green under `cargo test`.

---

## Phase 7: User Story 7 — Telemetry Logging Toggle (Priority: P3)

**Note (F4 — ordering is advisory)**: US7 (Logging) is implemented before US6 (Voice) to allow time for T005 (Docker shared volume) to be verified against the actual infra before Phase 8 hub code references the directory. T005 is in Phase 1 (Setup) so it is technically available, but confident verification of the volume mount is best done before writing the hub endpoint that depends on it. This is a **recommended ordering, not a hard dependency** — the dependency graph below shows US6 and US7 as independent and parallelizable once Phase 1 is complete. If T005 is already verified by the time Phase 7 begins, US6 and US7 may be implemented in parallel. **I4 (T033 parallelism with T034b)**: T033 `[P]` (logging unit tests) may run in parallel with T034b (the Phase 8 voice profile gate research task) if T010 is complete — T034b is a research/verification task with no shared file dependencies. T033 and T034 (hub endpoint implementation) are NOT parallelizable; wait for T034b to complete before beginning T034.

**Goal**: Driver enables logging; `.ndjson` file is created with telemetry frames; Racing Engineer path unaffected.

**Independent Test**: quickstart.md SC-8.

- [X] T030 [US7] Implement `apps/tauri-client/src-tauri/src/telemetry/logger.rs` (create `telemetry/` module, register in `lib.rs`; FR-020 — isolated write path): async NDJSON log writer; bounded `mpsc` channel (capacity: 1000 `TelemetryLogFrame`s); on channel-full, drop the incoming frame + increment a dropped-frame counter + emit `telemetry:log-warning { reason: "channel-full" }` Tauri event (non-blocking — no await on the emit); new log file per session named `iracing-telemetry-{sessionId}-{YYYYMMDD-HHmmss}.ndjson` in `AppConfig.telemetry_log_dir` (create dir with `std::fs::create_dir_all` if absent); graceful stop: drain channel to empty before closing file handle; if disk write fails, emit `telemetry:log-warning { reason: "disk-full", detail: err }` and stop logging. **E2 (drain-timeout event)**: When the drain timeout fires (T031/A3) and frames are discarded, emit a `telemetry:log-warning { reason: "drain-timeout", framesDiscarded: n }` Tauri event so T032 can surface this to the driver. This is distinct from "channel-full" (frames dropped during active logging) and "disk-full" (write failure); the drain-timeout event means frames that were still queued when toggle-off was received could not be flushed within the 500ms window. **U4 (sessionId source)**: `sessionId` in the log filename and in each `TelemetryLogFrame` is sourced from the iRacing `SessionNum` telemetry variable, extracted from the first telemetry frame received after session start. Use the string `"unknown"` as a placeholder until the first telemetry frame arrives — this prevents file creation from blocking on a value that may be delayed by a few frames. Once `SessionNum` is received, it becomes the canonical `sessionId` for the life of that session, used in every subsequent `TelemetryLogFrame`. **C1 (SC-008 measurement instrumentation)**: while logging is active, emit a 1Hz structured debug log `{ event: "telemetry-log-channel-depth", depth, capacity }` (current channel occupancy) — this is the observable T040 uses to confirm the ~80%-capacity load condition for the SC-008 P99 check. One atomic read per second; downgrade to trace level after M10 if noisy. **A2 (no file rename)**: the already-created log file is NOT renamed when the real `SessionNum` arrives — a filename of `iracing-telemetry-unknown-{timestamp}.ndjson` is acceptable and expected when the file is created before the first frame; the timestamp component keeps filenames unique, and the frames inside carry the canonical `sessionId` for analysis tooling. Renaming an open file mid-write is not worth the cross-platform risk.
- [X] T031 [US7] Implement `toggle_telemetry_logging(enabled: bool)` Tauri command in `apps/tauri-client/src-tauri/src/commands.rs`: send enable/disable signal to the logger task; persist `AppConfig.telemetry_logging_enabled`; return `Ok(())`. If enabling and a session is already active, open the log file immediately; if no session active, open on next session start. **C3 (stop behavior on toggle-off)**: On toggle-off, the logger MUST drain the bounded channel (flush all queued frames) and close the file handle immediately — it does NOT wait for the session to end. The "session boundary" phrasing in US7 acceptance scenario 2 refers to data integrity (no mid-frame writes), not deferred stop: the channel drain ensures all frames already enqueued before the toggle-off signal are written, then the file closes cleanly. This is "immediate drain-and-close" not "wait until session ends." **A3 (drain timeout — US7 scenario 2 bound)**: The drain MUST complete within 500ms of receiving the stop signal (or within 100ms after the last frame is enqueued if the channel is already empty, whichever is shorter). Implement using `tokio::time::timeout(Duration::from_millis(500), drain_future)` wrapping the channel-drain future; if the timeout fires, close the file handle immediately (frames in the channel after the timeout are discarded) and emit a `warn` log `{ event: "telemetry-log-drain-timeout", framesDiscarded: n }`. **U2 (readonly filesystem — distinct from creation failure)**: T031 MUST distinguish two error cases at path validation time: (a) "cannot create directory" — `create_dir_all` returns `Err` (path invalid, permission denied at directory creation) → `Err("cannot create log directory: [reason]")`; (b) "directory exists but not writable" — `create_dir_all` succeeds but a write-permission probe (attempt to create and delete a temp file) fails → `Err("log directory is not writable: [reason]")`. These MUST be distinct user-facing error messages in T032's warning banner. **C4 (path validation at toggle time)**: When enabling, attempt `std::fs::create_dir_all(&config.telemetry_log_dir)` and a write-permission probe before setting the enabled flag — return `Err("cannot create log directory: [reason]")` if the path is invalid or not writable; this closes the FR-026 gap where an invalid path would only fail at session start. **Constitution V (D2)**: on any failure path, return `Err` with a specific reason and emit a structured `warn` log `{ event: "telemetry-logging-toggle-failed", reason }` — no silent failures.
- [X] T032 [US7] Wire the Logging tab in `apps/tauri-client/src/pages/Setup.tsx`: toggle switch bound to `toggle_telemetry_logging()`; display `AppConfig.telemetry_log_dir` path (read-only text); subscribe to `telemetry:log-warning` events and show a warning banner with the specific reason — map each `reason` to a distinct message: `"channel-full"` → "Logging buffer full — some frames were dropped"; `"disk-full"` → "Disk write failed — logging stopped: [detail]"; `"drain-timeout"` → "Logging stopped — [n] queued frames could not be flushed in time." **E2 (drain-timeout reason required)**: The `drain-timeout` mapping MUST be implemented alongside the `channel-full` and `disk-full` cases — it is NOT optional. **C1 (sentinel display verification)**: Confirm the displayed path is the *resolved* absolute path (e.g., `/Users/driver/Library/Application Support/com.iracing-engineer/logs/telemetry`), NOT the sentinel `""` — if the path is empty or `""`, show an error state "Log directory path could not be resolved" rather than a blank field. The startup init flow in T001 should have already resolved and persisted the path; if it did not, this display check surfaces the failure. **G2 (toggle disabled on sentinel — FR-019/U3)**: While the displayed path is the sentinel `""`/empty (unresolved), the logging toggle MUST be rendered disabled (greyed out, non-interactive) alongside the error state — the driver MUST NOT be able to enable logging with no valid destination. The toggle becomes interactive only when a resolved absolute path is present.
- [X] T033 [P] [US7] Unit tests for `logger.rs` in `apps/tauri-client/src-tauri/src/telemetry/` tests: channel-full drops frame without panic and emits warning event; log file is created when logging is enabled with a session active; file is properly closed (not truncated) when logging is disabled; `TelemetryLogFrame` round-trips through NDJSON serialization correctly. **A3 (drain timeout test — required)**: Add a test that sends N frames to a full channel, then sends the stop signal, and asserts the drain completes within 500ms (use `tokio::time::timeout` in the test assertion). Also add: if the channel is already empty when stop is received, assert drain completes within 100ms. These correspond directly to the US7 scenario 2 dual-condition bound. **G4 (path-validation error distinction — T031 U2/C4)**: Add two cases for `toggle_telemetry_logging(true)` path validation: (a) an invalid/uncreatable path (e.g., a component under a nonexistent root or permission-denied parent) → `Err` beginning "cannot create log directory:"; (b) a directory that exists but is not writable (create a temp dir, remove write permission) → `Err` beginning "log directory is not writable:". These are two distinct user-facing messages that T032 renders differently — without both cases the distinction is untested.

**Checkpoint**: SC-8 passes — log file created, frames appear correctly, Tier 1 alert latency unaffected.

---

## Phase 8: User Story 6 — Voice Profile Upload (Priority: P3)

**Goal**: Driver uploads MP3, hub validates and stores it, test clip plays with cloned voice.

**Independent Test**: quickstart.md SC-7, SC-7b, SC-7c.

**Prerequisite**: T005 (shared Docker volume) must be complete and verified before writing hub code in this phase.

- [ ] T034b [US6] **PHASE GATE (I3 — pre-implementation constraint)**: Before any implementation in this phase begins, a developer MUST confirm the actual Chatterbox voice cloning duration constraints from the Chatterbox documentation or by testing the service directly. The spec Assumptions state 3–60 seconds. If the actual limits differ, (a) update `data-model.md` Validation Rules to reflect the confirmed limits, (b) update quickstart.md SC-7c with the correct expected error message, (c) update the named constants `minVoiceProfileDurationSecs` / `maxVoiceProfileDurationSecs` in `engineer-config.json` (created by T004/U1 in Phase 1 with the assumed 3–60 values — this step revises the VALUES only if the confirmed limits differ) before implementing T034, (d) update T038's hard-coded duration test expectations (the "< 3s" and "> 60s" cases), and (e) update plan.md Phase G's gate text ("duration < 3s and > 60s rejected") — I7: an earlier version of this list omitted (d) and (e), which would have left stale test expectations contradicting the confirmed limits. Do NOT proceed to T034 until this confirmation is documented. This task has no code deliverable — its only output is recorded confirmation (in a comment or PR description) and, if needed, spec/doc updates. **I2 (Docker volume mount verification)**: Also verify the shared volume is actually mounted and accessible (not just defined in the YAML): run `docker compose up -d hub-server chatterbox && docker exec hub-server ls /data/chatterbox/reference` (or the actual configured path). If the `ls` command fails or the container exits, the volume mount is broken and MUST be fixed before T034 begins — hub code that writes to an unmounted path will fail silently at runtime.
- [X] T034 [US6] Implement `POST /api/voice-profile` endpoint (FR-021; FR-022 hub-side gates) in `apps/hub-server/src/api.ts` per `contracts/hub-voice-profile.md`: use Hono's multipart body parser to receive `audio` field; validate MIME type is `audio/mpeg` (422 if not: `{ error: "invalid-format", message: "File must be an MP3 (audio/mpeg)" }`); **I5 (hub magic byte check — second gate)**: additionally validate the file's content by checking the first 4 bytes of the received buffer against the MP3 magic byte sequences defined canonically in `data-model.md` "MP3 magic byte sequences — shared reference" (currently `FF FB`, `FF F3`, `FF F2`, or `49 44 33` for ID3-tagged MP3s; data-model.md is the single source of truth if the accepted set ever changes — C1) — a file that passes the MIME header check but fails the content check MUST be rejected with the same 422 `invalid-format` response; the hub is a second gate after the Tauri client magic byte check in T036 and MUST NOT rely on the MIME header alone; **Prerequisite: T034b must be completed first** — `minVoiceProfileDurationSecs` and `maxVoiceProfileDurationSecs` MUST already be confirmed and recorded as named constants in `engineer-config.json` before implementing the duration validation below. Use `music-metadata` to parse duration from the buffer (422 if < min: `{ error: "duration-out-of-range", message: "Audio must be between {min} and {max} seconds (got Xs)" }`, same for > max); generate filename `profile-{ISO8601timestamp}.mp3`; write buffer to `{engineer-config.chatterboxReferenceAudioDir}/{filename}` (500 on write failure); write `hub:config:voice-profile = { filename, uploadedAt, durationSeconds }` to Redis (include durationSeconds so get_voice_profile() can return it without re-reading the file — see data-model.md hub:config:voice-profile schema); update in-memory `chatterboxVoiceFile`; synthesize a test clip ("Racing engineer online. Voice profile active.") using `TtsClient` with the new reference file; return 200 JSON response per contract. **U2 (partial-write atomicity — file and Redis consistency)**: If the filesystem write to `{chatterboxReferenceAudioDir}/{filename}` succeeds but the subsequent Redis `hub:config:voice-profile` write fails: (a) attempt to delete the partially-written file (`fs.unlink`) before returning, (b) return 500 with `{ error: "voice-profile-sync-failed", message: "File saved but Redis update failed — retry upload" }`, (c) do NOT update the in-memory `chatterboxVoiceFile`. Conversely, if Redis write succeeds but the file write fails, also return 500 — do NOT update `chatterboxVoiceFile` in either partial-failure case. Both writes must succeed for the in-memory state to be updated. **C4 (mechanism distinction — T034 vs T037)**: The test clip synthesized here (hub-side, at upload time) and returned as `testClipUrl` is for immediate upload confirmation only. The "Test Voice" button in T037 uses `test_audio_playback()` (the existing M4 Tauri command) which triggers a new synthesis on demand at any time after upload. These are two separate mechanisms for two separate flows — T034's `testClipUrl` is ephemeral; T037's `test_audio_playback()` is persistent and reusable. **C4b (hub budget ≤ 50s — SC-006 margin, C3)**: The entire endpoint — file validation, disk write, Redis write, TTS synthesis, and 200 response — MUST complete within 50 seconds (measured from first byte received to 200 response sent). Spec SC-006's 60-second budget is USER-side (upload click → audible test clip) and includes upload transfer, response handling, and playback start on top of hub processing — a hub budget equal to the full 60s would leave zero client-side margin, so the hub gets 50s with ~10s reserved for the client. T040's user-side measurement is the binding SC-006 check. If Chatterbox voice-cloning synthesis consistently exceeds ~45 seconds on a 30-second reference clip, the 50s hub budget cannot be met and SC-006 must be flagged during T034b confirmation testing before implementing this endpoint.
- [X] T035 [P] [US6] Extend `apps/hub-server/src/engineer/tts-client.ts` to read `hub:config:voice-profile` from Redis on startup: if key exists and the referenced file exists on disk AND has size > 0, set `chatterboxVoiceFile` to the stored filename; if the file is missing OR is 0 bytes (e.g., failed/interrupted upload left an empty file), emit a `warn` log `{ event: "voice-profile-invalid", reason: "file missing or empty" }` and fall back to `engineer-config.json`'s `chatterboxVoiceFile` value.
- [X] T036 [US6] **F2 (sequential dependency on T034)**: T036's local magic byte validation can be implemented and unit-tested independently of T034, but end-to-end integration testing (full upload flow) requires T034's hub endpoint to exist. T036's unit tests cover only the magic byte check and error return paths with a mocked HTTP client. Implement two Tauri commands (FR-021 upload path; FR-022 client-side gate; FR-024 display data) in `apps/tauri-client/src-tauri/src/commands.rs`: (1) `get_voice_profile()` — read `hub:config:voice-profile` from Redis and return `Result<Option<VoiceProfileState>, String>` where `VoiceProfileState = { filename, uploadedAt, durationSeconds }`: `Ok(Some(state))` when the key exists, `Ok(None)` when Redis is reachable but the key is absent (no profile uploaded), `Err(reason)` when Redis is unreachable — **I1: these are two distinct UI states and MUST NOT be collapsed into a single `None`** (an earlier draft returned `None` for both, making the two required messages unrenderable). Caller renders "Default voice (no profile uploaded)" on `Ok(None)` and "Redis unreachable — profile status unavailable" on `Err` (message renamed from "Hub offline" — the failing dependency is Redis, not the hub process). Emit a structured `warn` `{ event: "voice-profile-read-failed", reason }` on the `Err` path. Contract section added to `contracts/tauri-commands.md` (2026-07-08); (2) `upload_voice_profile(file_path: String)` — read file bytes from `file_path`; validate MP3 magic bytes against the canonical list in `data-model.md` "MP3 magic byte sequences — shared reference" (currently `FF FB`, `FF F3`, `FF F2`, or `49 44 33` — ID3 tag; data-model.md is the single source of truth — C1); if wrong format return `Err("File must be an MP3")`; POST as `multipart/form-data` with `audio` field to `{hub_url}/api/voice-profile` with an explicit **90-second client-side timeout** (the hub's processing budget is 50s per T034/C4b — SC-006's 60s is the user-side budget; the 90s client timeout covers the hub budget plus network transfer margin — I3); deserialize response into `VoiceProfileResult { filename, uploadedAt, durationSeconds, testClipUrl }`; return `Ok(VoiceProfileResult)`, `Err(hub_error_message)` on non-2xx, or `Err("Upload timed out — hub did not respond within 90s")` on timeout. **C3 (Rust-side unit tests — ownership corrected, I1)**: `upload_voice_profile()` is a Rust Tauri command — its unit tests belong in the Rust `commands.rs` suite and CANNOT live in T038 (a hub TypeScript suite cannot test Rust; an earlier draft misassigned the timeout test there). Add as part of T036: (a) wrong magic bytes → `Err("File must be an MP3")` with NO network request made; (b) hub returns non-2xx → `Err(hub_error_message)` surfaced verbatim; (c) timeout path — mock the POST with a delay exceeding the timeout (make the timeout injectable/test-configurable so the test doesn't wait 90 real seconds) → `Err("Upload timed out — hub did not respond within 90s")`; (d) every `Err` path emits the D2 structured warn log. These are the client-side unit deliverables that F2 references. **Constitution V (D2)**: on any `Err` path (format check, hub POST failure, hub error response), emit a structured `warn` log `{ event: "voice-profile-upload-failed", reason }` before returning.
- [X] T037 [US6] Wire the Voice tab (FR-023, FR-024) in `apps/tauri-client/src/pages/Setup.tsx`: on mount, call `get_voice_profile()` (added in T036) to load current profile from Redis — three distinct states per the I1 contract: display filename + timestamp + duration on `Ok(Some(...))`, "Default voice (no profile uploaded)" on `Ok(None)`, or "Redis unreachable — profile status unavailable" on `Err`; **E4 (not from AppConfig)**: voice profile state is intentionally NOT stored in `AppConfig`; always fetch from Redis via `get_voice_profile()` on mount; file picker button (MP3 filter: `.mp3`) → invoke `upload_voice_profile(filePath)` → show progress indicator while awaiting → on success display new filename + timestamp + duration (from `VoiceProfileResult` — includes `durationSeconds`; do NOT wait for a remount/get_voice_profile() call to show the duration, show it immediately from the upload response); "Test Voice" button → invoke `test_audio_playback()` (the existing M4 command — after a successful upload the hub's TTS client uses the new reference profile, so the standard audio test naturally plays the cloned voice). **U4 (mechanism clarification)**: `test_audio_playback()` triggers real-time Chatterbox TTS synthesis (not a pre-recorded WAV file) — because the hub's in-memory `chatterboxVoiceFile` was updated by the upload endpoint (T034), the synthesized output will use the cloned voice. Add a manual verification note to quickstart.md SC-7 step 7: "Confirm the sample phrase sounds like the uploaded voice, not the default Chatterbox voice." Subscribe to upload errors and display inline with specific message. **Note (C4)**: `testClipUrl` in the upload response is ephemeral and used for display/confirmation purposes only; do not cache it for deferred playback — use `test_audio_playback()` for on-demand playback at any time after upload.
- [X] T038 [P] [US6] Unit tests for `POST /api/voice-profile` in `apps/hub-server/tests/unit/api/voice-profile.test.ts`: non-MP3 MIME → 422 with `invalid-format`; valid MIME but wrong magic bytes → 422 with `invalid-format` (validates hub second gate from I5); valid MP3 with duration < 3s → 422 with `duration-out-of-range`; valid MP3 with duration > 60s → 422 with `duration-out-of-range`; valid MP3 within range → 200 + Redis write + correct filename generated (mock filesystem write + mock TtsClient). **B4 (testClipUrl not stored, durationSeconds required)**: Add an assertion that after a successful upload, `hub:config:voice-profile` Redis key contains `{ filename, uploadedAt, durationSeconds }` — `durationSeconds` MUST be present (required by `get_voice_profile()` return contract per `data-model.md`); `testClipUrl` MUST be absent (ephemeral, never persisted). And add an assertion to T037's frontend behavior: after invoking `upload_voice_profile()` successfully, neither `AppConfig` nor the persisted `VoiceProfileState` contains a `testClipUrl` field — it exists only in the transient `VoiceProfileResult` response type. **E3 (in-memory state transition test)**: Add a test that asserts after a successful `POST /api/voice-profile`, the mocked `TtsClient.synthesize()` call made AFTER the upload uses the new `chatterboxVoiceFile` value (not the startup-loaded value). This verifies the in-memory state handoff that T040/F3 checks manually — both guards are needed.

**Checkpoint**: SC-7, SC-7b, SC-7c pass — MP3 upload → test clip plays with cloned voice; non-MP3 and out-of-range files rejected with correct messages.

---

## Phase 9: Polish & End-to-End

**Purpose**: Save failure handling, hub sync warning, end-to-end validation across all stories.

- [X] T039 Implement save failure surface in `apps/tauri-client/src/pages/Setup.tsx` (FR-027): update `save_config()` invocation to handle `Err` return — if local save fails (disk error, permission), retain all tab form state as-is, show an inline error banner at the top of the settings page with the specific cause ("Cannot write to config file: [reason]"), disable Save button until the condition is resolved. Add a second banner type for hub sync warnings (returned as a non-error result field): "Settings saved locally. Hub sync failed: [reason]" — this banner is dismissable and does not block continued use. **Note (I3 — Save button scope)**: The Save button is disabled ONLY when local disk save fails and the error condition is unresolved. Hub sync warnings (dismissable banner) do NOT disable the Save button — the driver can continue saving settings normally while the hub sync warning is visible. **C2 (inverse test — no disk rollback on hub failure)**: Add a unit test or integration test asserting that when local disk save succeeds but the Redis hub sync pipeline fails, the config file on disk still contains the new values (i.e., the successful local save is NOT rolled back by the hub sync failure). Mock the Redis client to throw after the disk write succeeds, then read the config file and assert the new values are present. **B2 (hub sync retry on explicit retry)**: When the driver encounters a local disk save failure and later clicks Save again (retry), the command MUST attempt hub sync after the local save succeeds — do not skip hub sync on retries. The skip-hub-sync rule only applies when local disk save fails in the same call; a new Save click starts a fresh save_config() invocation and hub sync MUST be attempted if local save returns Ok(()). This ensures hub sync is not silently skipped indefinitely after a transient disk error is resolved.
- [ ] T040 [P] Verify deployment of all features against quickstart.md: run quickstart SC-1 (audio), SC-2 (LLM model change), SC-3 (PTT binding), SC-3b (accessibility error + "Open Accessibility Settings" button), SC-4 (personality suppress), SC-5 (debug readout), **SC-5b (Diagnostics redirect to Debug tab)**, SC-6 (save failure), SC-6b (hub sync failure + stale-value limitation), SC-7 (voice upload — quickstart scenario; distinct from spec success criterion SC-007 "settings persist"), SC-7b (format reject), SC-7c (duration reject), SC-8 (logging) — 13 scenarios total (I3: this enumeration is authoritative; do not rely on bare counts elsewhere). Document any failures. **Timing checks against spec success criteria (C1/E1/E2/I4)**: spec SC-001 — time from first opening Audio tab to completing device selection + test playback, must be under 3 minutes; spec SC-003 — time from "Set PTT Key" click to PTT-active indicator confirmation (the Hotkeys tab indicator per T023/A1 — NOT the mic meter, which is always-on), must be under 30 seconds; spec SC-005 — time from an actual Redis stream lag event to the warning appearing in the debug panel, must be under 1 second; spec SC-006 — time from clicking upload on a 10–30s MP3 to hearing the test clip, must be under 60 seconds; spec SC-007 — save all settings, force-quit, relaunch the app, confirm all fields display their saved values with none reverted to defaults; spec SC-008 — with telemetry logging enabled and the logging channel loaded to ~80% capacity, measure Tier 1 alert delivery latency against the logging-disabled baseline: P99 MUST NOT increase by more than 50ms (the quantitative SC-008/E3 threshold — a subjective "no measurable increase" is explicitly NOT an acceptable pass condition per spec SC-008; if the bound cannot be held, tune the bounded-channel capacity per T030 before merge). **C1 (measurement procedure — makes the check reproducible)**: (1) drive the channel to ~80%: enable logging and inject synthetic telemetry at an elevated rate (session replay or a debug injection loop) until the `telemetry-log-channel-depth` metric (T030/C1) reads ~800/1000 sustained; (2) capture latency via structured timing on the Tier 1 alert path — timestamp at triggering telemetry event receipt and at audio dispatch, logged as `{ event: "tier1-alert-latency", ms }` (add this two-line structured log emission if absent — it is instrumentation, not a new subsystem); (3) collect ≥100 alert samples per condition (logging-off baseline vs. logging-at-80%) and compute P99 from the logs. Pass: P99(logging) − P99(baseline) ≤ 50ms. **E2 (FR-030 nav-link audit)**: Before signing off on T040, confirm the FR-030 redirect is complete: run `grep -r '/diagnostics' apps/tauri-client/src/` and verify every reference site found routes correctly through the redirect wrapper added in T009. Document each reference site and its resolution in a comment or PR note. **F2 (cross-tab accessibility after PTT error)**: After triggering `ptt:accessibility-denied` in SC-3b, confirm that all other tabs (Audio, Connection, Personality, Debug, Voice, Logging) remain fully accessible and functional — the error state MUST be contained to the Hotkeys tab and MUST NOT prevent navigation to or interaction with any other tab. **G3 (FR-017/U4 — sustained hub loss mid-session)**: With an active session and the Debug tab open, stop the hub server for >60 seconds — confirm the debug panel keeps updating at ~1Hz with `hubConnected` shown as "Disconnected" throughout (snapshot emission MUST NOT stall due to hub loss alone); then restart the hub and confirm the indicator returns to "Connected" within the 10s stale window plus one probe interval. **E1 (in-flight model switch manual check)**: Save a new LLM model name while the hub is actively processing a Tier 3 synthesis request. Confirm in the hub server logs that: (a) the in-flight synthesis completes with the old model name, and (b) the next Tier 3 request uses the new model name. No debug panel UI change is expected — this is a hub behavioral verification only, confirming the T021/E1 in-flight-switch isolation guarantee. **F3 (in-memory TTS state handoff after upload)**: After completing quickstart SC-7 (successful voice upload), click "Test Voice" without restarting the hub — confirm the test clip sounds like the uploaded voice (not the default Chatterbox voice). This verifies that T034's hub endpoint correctly updated `chatterboxVoiceFile` in memory and T037's `test_audio_playback()` is using that updated state without requiring a restart. **C1 (plan performance-goal spot check)**: While running the save flows above, spot-check that a local `save_config()` completes in under 200ms (the plan's Performance Goals target; hub Redis sync is async and excluded from the measurement) — a timestamp log or devtools timing suffices; document the observed value. This closes the gap where the plan stated the goal but no task measured it.
- [X] T041 [P] Run `npm run build && npm run typecheck` workspace-wide; `cargo test`; `rustfmt`; `clippy`; ESLint + Prettier; `npm test` (all workspaces including `apps/tauri-client`) — all must pass with zero errors or warnings. Fix any issues found. **C1 (Vitest CI inclusion check)**: Confirm that `npm test` at the repo root (or the equivalent CI command) includes `apps/tauri-client`'s Vitest suite — run `npm test --workspace=apps/tauri-client` explicitly and verify T009's redirect test appears in the output. If `apps/tauri-client` is not included in the root `npm test` command, add it. This mirrors T042/D1's CI inclusion check for `tier3-synthesizer.test.ts`.
- [ ] T042 [P] Confirm Definition of Done checklist in `plan.md` — mark all items complete. Verify: deprecated M4 fields gone, `hub:config:llm` key matches contract, `POST /api/voice-profile` response matches contract, `infra/docker-compose.yml` shared volume present, all 13 quickstart scenarios passing (SC-1 through SC-8 plus SC-3b, SC-5b, SC-6b, SC-7b, SC-7c — use T040's enumeration as the authoritative list, not a bare count; I3). **F1 / C3 (BLOCKING — Constitution III)**: Confirm that T043 was completed AND that the unit test asserting `llm_model` appears in the `engineer_events` audit row (added in T043's D1 clause) passes in CI. "T043 complete" means the CI test is green — not merely that the migration file was written or the columns were verified manually. If the write-path test does not exist, is skipped, or is failing: do NOT merge. This is the enforcement mechanism that makes the Constitution III gate meaningful. **D1 (CI inclusion verification)**: Before marking T042 complete, run `grep -r 'tier3-synthesizer' package.json .github/` (or the equivalent CI config files for this project) and confirm that `tier3-synthesizer.test.ts` is included in the test suite that CI executes — it MUST NOT be in a `--ignore`'d glob, a separate test suite that CI does not run, or excluded by a `testPathIgnorePatterns` entry. **C2 / D2 (CI config may not exist — resolve before any T017–T021 work begins)**: BEFORE starting T017, verify CI status: run `ls .github/workflows/` and check whether any workflow file runs `npm test`. (a) If CI exists and runs `npm test` → T042/D1 grep will confirm inclusion; no additional action needed. (a2) **CONFIRMED ACTUAL STATE (I1, workflow contents verified 2026-07-08)**: CI exists (`.github/workflows/ci.yml`) but runs ONLY `cargo test -p iracing-engineer-lib` and `npm run build -w apps/hub-server` — no workflow step runs `npm test`, so the tier3-synthesizer write-path test would never execute in CI as-is (the earlier decision tree had no branch for this state). Resolution: add an `npm test -w apps/hub-server` step to `ci.yml` as part of T043's gate work, BEFORE any T017–T021 merge. Only if that step cannot be added does branch (b) apply. (b) If CI does NOT exist → add a Husky pre-commit hook as part of T042 setup (not as a last resort): run `npm install --save-dev husky` in repo root, add `.husky/pre-commit` that runs `npm test --workspace=apps/hub-server -- --testPathPattern=tier3-synthesizer`. Install the hook with `npx husky install`. This MUST be done before any T017–T021 code is committed. (c) **Last resort only (both CI and pre-commit unavailable)**: Add a mandatory PR checklist item — but EXPLICITLY note in the PR that this is a Constitution III enforcement gap and add a follow-up ticket to add CI. A PR checklist is human-optional and does NOT provide automated enforcement. **D2 gate**: Add a sub-step to T042's DoD check: "Confirm one of (a), (b), or (c) above is in place for `tier3-synthesizer.test.ts` BEFORE marking T042 complete."

**Checkpoint**: All quickstart scenarios pass, workspace builds cleanly, Definition of Done satisfied.

---

## Story Dependency Graph

```
T001-T010 (Phase 1: Setup)
    │
    ├── T011-T016 (US1: Audio) ← can start immediately after T010
    │
    ├── T043 (Constitution III gate — BLOCKING) → T017-T021 (US2: Connection/LLM) ← T043 MUST complete before ANY T017-T021 task
    │
    ├── T022-T024 (US3: PTT) ← after T010
    │
    ├── T025-T026 (US4: Personality) ← after T010
    │
    ├── T027-T029b (US5: Debug) ← after T010
    │
    ├── T030-T033 (US7: Logging) ← after T010
    │
    └── T034b (gate) → T034 → T035/T036/T037/T038 (US6: Voice) ← after T005 specifically (Docker volume)
         │
         └── T039-T042 (Polish) ← after all US phases
```

> **Graph note (F5)**: within US6, the sequential arrow `T034 → T035/T036/T037/T038` shows the END-TO-END dependency only — T035 and T036's unit-scope work (startup Redis read, magic-byte validation, mocked unit tests) may begin before T034 completes (see T035's `[P]` marker and T036/F2); only E2E integration testing waits for T034's endpoint.

US1, US2, US3, US4, US5, US6, US7 are **all independent** of each other once Phase 1 (T001–T010) is complete. They can be implemented in parallel. US4 is the smallest (1–2 tasks, mostly verification of existing M5 work).

## Parallel Execution Example (US1 + US2 simultaneously)

After T010 passes, two engineers (or two sessions) can implement US1 and US2 in parallel:

**Stream A (US1 — Audio)**:
T011 → T012+T013 (parallel) → T014 → T015+T016 (parallel)

**Stream B (US2 — Connection/LLM)**:
T017 → T018+T019 (parallel) → T020 → T021

## Implementation Strategy

**MVP** = Phase 1 (Setup) + Phase 2 (US1: Audio) + Phase 3 (US2: Connection/LLM) — both P1 stories.

After MVP, the driver can configure audio devices and connection settings (including LLM endpoint) without editing files, and the LLM model change takes effect without restart. This alone unblocks real-world use of M5.

**Phase 4 (US3: PTT) + Phase 5 (US4: Personality)** complete the P2 story set — the driver can bind PTT keys and tune personality without touching Redis.

**Phase 6 (US5: Debug)** closes out P2 stories — telemetry health visible in-app.

**Phase 7–8 (US6, US7: Logging + Voice)** are P3 enhancements that can be deferred if needed.
